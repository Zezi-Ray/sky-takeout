<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <insert id="insertBatch" parameterType="list">
        INSERT INTO shopping_cart
            (user_id, dish_id, setmeal_id, name, image, dish_flavor, number, amount, create_time)
            VALUES
        <foreach collection="shoppingCartList" item="item" separator=",">
            (#{item.userId}, #{item.dishId}, #{item.setmealId}, #{item.name}, #{item.image},
             #{item.dishFlavor}, #{item.number}, #{item.amount}, #{item.createTime})
        </foreach>
    </insert>

    <select id="list" resultType="com.sky.entity.ShoppingCart">
        SELECT * FROM shopping_cart
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="dishId != null">
                AND dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                AND setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                AND dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

</mapper>